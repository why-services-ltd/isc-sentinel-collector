# Python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.coverage

# Azure Functions Core Tools build artefacts, created locally by `func publish`
.python_packages/
bin/
obj/

# Azure / Bicep build output
*.json.bak
main.json
what-if-*.txt

# Local settings that may contain secrets
local.settings.json
.env

# Deployment parameter files carry real tenant values (ISC hostname, alert
# addresses, resource names). infra/main.bicepparam is the committed example
# with placeholders; every other parameter file stays local by default.
infra/*.bicepparam
!infra/main.bicepparam

# Internal documents. These describe the deployed estate, its identifiers and
# its credential-handling design once their placeholders are filled in, so they
# are kept out of the repository by default rather than by remembering to.
*.docx
*.doc
*.pdf
*.xlsx
*.pptx
~$*

# Local editor / assistant configuration
.claude/
.vscode/
.idea/

# macOS
.DS_Store
